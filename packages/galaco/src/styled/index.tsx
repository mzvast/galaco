import styled, {
    css,
    keyframes,
    createGlobalStyle,
    isStyledComponent,
    ThemeConsumer,
    // ThemeProvider,
    withTheme,
    ServerStyleSheet,
    StyleSheetManager
} from 'styled-components';
import {
    prop,
    theme,
    palette,
    ifProp,
    ifNotProp,
    switchProp,
    withProp
} from 'styled-tools';

export {default as use} from 'reuse';
export {
    styled,
    css,
    keyframes,
    createGlobalStyle,
    isStyledComponent,
    ThemeConsumer,
    // ThemeProvider,
    withTheme,
    ServerStyleSheet,
    StyleSheetManager,
    prop,
    theme,
    palette,
    ifProp,
    ifNotProp,
    switchProp,
    withProp
};
export default styled;
